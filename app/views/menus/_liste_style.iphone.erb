<% @menus.each do |menu| %>
<li>
    <%= link_to %Q{#{menu.title}}, :controller => "menus",  :action => "show", :id => menu %>
</li>
<% end %>
<%= content_tag :li, link_to("Plus de menus", menus_path(:page => @menus.next_page), :target => "_replace") if @menus.next_page %>
