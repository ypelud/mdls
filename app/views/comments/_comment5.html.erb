<div class="byline">
<% menu=Menu.find(Comment.find_commentable(comment.commentable_type, comment.commentable_id).id) %>
<%= link_to %Q{#{menu.title}}, :controller => "menus",  :action => "show", :id => menu %>
<div><%= comment.comment %></div>
<%= User.find(comment.user_id).login %> : <%= comment.created_at.to_formatted_s(:short) %> 
</div>
